import { BrandpayDriver } from '../utils/brandpayDriver';
export declare function createBrandpayOpenSettings({ clientKey, customerKey, redirectUrl, }: {
    clientKey: string;
    customerKey: string;
    redirectUrl?: string;
}): Promise<void>;
export declare function createBrandpayRequestPayment({ clientKey, customerKey, redirectUrl, brandpayPaymentParams, }: {
    clientKey: string;
    customerKey: string;
    redirectUrl?: string;
    brandpayPaymentParams: ReturnType<InstanceType<typeof BrandpayDriver>['getRequestPaymentParams']>;
}): string;
