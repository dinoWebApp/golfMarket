import { Device } from '@payment-widget/models';
interface Response {
    clientKey: string;
    usable: boolean;
    ui: {
        highlightColor: string;
    };
}
export declare function fetchBrandpayConfig(clientKey: string, device: Device): Promise<Response | null>;
export {};
