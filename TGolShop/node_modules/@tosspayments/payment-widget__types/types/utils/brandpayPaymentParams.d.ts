import { PaymentParams, PaymentParamsWithoutUrl, PaymentParamsWithUrl } from '@tosspayments/brandpay-types';
declare type PickMethod = 'orderId' | 'orderName' | 'successUrl' | 'failUrl' | 'customerEmail' | 'taxFreeAmount';
declare type Params = Pick<PaymentParams, PickMethod>;
export declare class BrandpayPaymentParams {
    orderId: Params['orderId'];
    orderName: Params['orderName'];
    successUrl: Params['successUrl'];
    failUrl: Params['failUrl'];
    customerEmail: Params['customerEmail'];
    taxFreeAmount: Params['taxFreeAmount'];
    constructor({ orderId, orderName, customerEmail, failUrl, successUrl, taxFreeAmount }: Params);
    get(): Pick<PaymentParamsWithUrl, PickMethod>;
    get(): Pick<PaymentParamsWithoutUrl, PickMethod>;
}
export {};
